services:
    db:
        container_name: mydb
        image: mysql:latest
        ports:
            - "32001:3306"
        environment:
            MYSQL_ROOT_PASSWORD: root      
            MYSQL_USER: admin
            MYSQL_PASSWORD: admin
            MYSQL_DATABASE: messages
        healthcheck:
            test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
            interval: 10s
            retries: 5
        volumes:
            - mysql_data:/var/lib/mysql
            - ./init-scripts:/docker-entrypoint-initdb.d
        networks:
            - mynetwork

    adminer:
        container_name: myadminer
        image: adminer:latest
        restart: always
        ports:
            - "8080:8080"
        environment:
            ADMINER_DEFAULT_SERVER: db
        networks:
            - mynetwork

    app:
        build: ./       
        deploy:
            mode: replicated
            replicas: 1  # Ejecutar 3 instancias de Flask
        networks:
            - mynetwork
        depends_on:
            - db

    nginx:
        image: nginx:latest
        depends_on:
            - app
        volumes:
            - ./nginx.conf:/etc/nginx/nginx.conf:ro  # Configuración del balanceador
        ports:
            - "4000:4000"
        networks:
            - mynetwork

    prometheus:
        container_name: prometheus
        image: prom/prometheus:latest
        volumes:
            - ./prometheus/config.yml:/etc/prometheus/prometheus.yml
        depends_on:
            - app
        ports:
            - "9090:9090"
        networks:
            - mynetwork

    mysql-exporter:
        container_name: mysql-exporter
        image: prom/mysqld-exporter
        command: 
            - --config.my-cnf=/cfg/.my.cnf
            - --mysqld.address=db:3306
        volumes:
            - "./.my.cnf:/cfg/.my.cnf"
        depends_on:
            - db
        ports:
            - 9104:9104
        networks:
            - mynetwork

    grafana:
        container_name: grafana
        image: grafana/grafana
        restart: unless-stopped
        environment:
            - GF_SECURITY_ADMIN_USER=admin
            - GF_SECURITY_ADMIN_PASSWORD=admin
        volumes:
            - ./grafana:/etc/grafana/provisioning/datasources
        depends_on:
            - prometheus
        ports:
            - 3000:3000
        networks:
            - mynetwork

networks:
    mynetwork:
        driver: bridge
        name: mynetwork
        
volumes:
    mysql_data:

